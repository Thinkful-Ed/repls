// Write your function 'findPerson'. Called with an array of people and a name, it should return a person object that matches

function findPerson(people, nameToMatch) {
  // loop through the collection of people
  // if the current person's name matches the nameToMatch
  // return the person
}


// This tests your code - you can ignore it for now!
require('./test.js');(void 0);