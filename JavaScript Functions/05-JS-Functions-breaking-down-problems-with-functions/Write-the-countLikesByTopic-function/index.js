// Use the pseudocode to help you write the 'countLikesByTopic' function

function countLikesByTopic(people, topic) {
  // Create a counter and set it to zero
  // Loop through the collection of people
  // Loop through the collection of likes for that person
  // If the like matches the topic
  // Increment the counter
  // return the counter
}

// This tests your code - you can ignore it for now!
require('./test.js');(void 0);