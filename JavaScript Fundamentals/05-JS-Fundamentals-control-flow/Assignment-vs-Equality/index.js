// Change this to assignment, instead of equality
let assignMe == 7;

// Change this to a check for equality
if (assignMe = 8) {
  console.log("This should not be logged. Change the assignment to a check for equality.")
}

// Change this to a check for equality too.
if (assignMe = 7) {
  console.log("The number was assigned correctly!");
}